#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define endl "\n"
const ll MOD = 1e9 + 7;

int main()
{
    ll i, j, n;
    cin >> n;
    vector<string> in;
    string s;

    for(i = 0; i < n; i++)
    {
        cin >> s;
        in.push_back(s);
    }

    vector<vector<ll>> dp(n + 1, vector<ll>());

}