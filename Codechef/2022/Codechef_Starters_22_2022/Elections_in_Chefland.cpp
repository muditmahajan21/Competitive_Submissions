#include <bits/stdc++.h>
#define ll long long int
#define endl "\n"
using namespace std;

int main()
{   
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll test;
    cin >> test;
    while(test--)
    {
        ll i, j, k, n, temp, count = 0, ans = 0, sum = 0;
        cin >> i >> j >> k;
        if(i > 50) cout << "A\n";
        else if(j > 50) cout << "B\n";
        else if(k > 50) cout << "C\n";
        else cout << "NOTA\n";
    }

return 0;
}
