#include<stdio.h>
#include<math.h>
#define ll long long int

int main()
{
    ll test;
    scanf("%lld", &test);
    while(test--)
    {
        ll n, i, j, k, temp, ans = 0, count = 0;
        scanf("%lld", &n);
        ll arr[n];
    }

return 0;
}